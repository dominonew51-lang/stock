# Luna 任务：A股、基金今日行情板块

## 1. 目标

在 Minimalism 资产总览页新增「A股 / 基金今日行情」板块，数据只来自用户当前持仓。

板块需要：

1. 汇总持仓中的 A 股、场内 ETF 和场外基金今日行情。
2. A 股开盘期间自动更新，休市后停止高频请求并保留收盘数据。
3. 对持仓中属于「红利」分类的 A 股股票展示日内走势图。
4. 对这些红利股计算并展示现价距离 MA250 的百分比：位于 MA250 上方显示正数，位于下方显示负数。
5. 保持现有 Notion 极简风格、A 股颜色习惯和手机端操作逻辑。

## 2. 口径与边界

### 2.1 红利股筛选

日内走势图仅包含同时满足以下条件的持仓：

```ts
holding.market === "A股"
&& holding.category === "红利"
&& quote.instrumentType === "stock"
```

场内 ETF 即使当前被标记为 `market: "A股"`，也不应被当作「红利股」。需要由行情接口返回明确的 `instrumentType`。

### 2.2 基金行情

- 场内 ETF：展示实时成交价和今日涨跌，可按 A 股交易时段更新。
- 场外基金：没有真实日内成交价，只展示最新确认净值、净值日期；不要生成伪日内线。
- 场外基金当日净值尚未公布时，明确显示“最新净值”及日期，不写“实时”。

### 2.3 MA250

- 使用最近 250 个已完成交易日的前复权收盘价计算。
- `MA250 = 250个交易日收盘价之和 / 250`。
- 实时距离：`(现价 - MA250) / MA250 × 100%`。
- 上方显示 `+x.xx%`，下方显示 `-x.xx%`。
- 不足 250 个有效交易日时显示 `—`，不得用不足 250 日的平均值冒充 MA250。
- 正数沿用 A 股习惯显示红色，负数显示绿色，股价本身保持黑色。

## 3. 建议的展示结构

位置：总资产卡片之后、资产走势之前。

### 3.1 板块头部

- 标题：`A股 / 基金今日行情`
- 市场状态：`交易中`、`午间休市`、`已收盘`、`休市`
- 最后更新时间，使用 Asia/Shanghai 时区
- 手动刷新按钮

### 3.2 今日行情摘要

展示持仓内全部 A 股、场内 ETF、场外基金，按两组呈现：

- `A股与场内ETF`
- `场外基金`

每项至少显示：

- 简称、代码
- 当前价或最新净值
- 今日涨跌额、涨跌幅；场外基金无可靠数据时显示 `—`
- 行情时间或净值日期
- 数据状态：实时、延迟、最新净值、暂不可用

### 3.3 红利股日内走势

每只红利股使用一张紧凑卡片，显示：

- 名称、代码
- 当前价、今日涨跌幅
- `距 MA250 +x.xx% / -x.xx%`
- 09:30–11:30、13:00–15:00 的日内线
- 昨收参考虚线
- 最后更新时间

桌面端两列，手机端单列。点击或触摸图表时，可显示最近数据点的时间和价格；第一版如果工期有限，触摸提示可列为次优先级，但走势图和 MA250 必须完成。

## 4. 后端数据任务

### Task A：抽离中国行情公共工具

将 `app/api/assets/route.ts` 中以下逻辑抽到可复用服务模块，例如：

```text
app/market-data/cn-market.ts
```

包括：

- 代码清洗与沪深交易所识别
- 东方财富请求
- 腾讯行情降级请求
- 超时、错误转换、数值校验
- 证券类型识别

不得在页面组件中直接调用第三方行情源。

### Task B：新增今日行情接口

建议新增：

```text
app/api/cn-market/route.ts
```

请求示例：

```text
GET /api/cn-market?codes=600023,600036,159696,021000
```

返回结构建议：

```ts
type CnMarketItem = {
  symbol: string;
  name: string;
  instrumentType: "stock" | "etf" | "fund";
  price: number | null;
  previousClose: number | null;
  changeAmount: number | null;
  changePercent: number | null;
  quoteAt: string | null;
  navDate?: string | null;
  intraday?: Array<{ time: string; price: number }>;
  ma250?: number | null;
  ma250DistancePercent?: number | null;
  source: "东方财富" | "腾讯行情";
  stale: boolean;
};

type CnMarketResponse = {
  marketState: "open" | "lunch" | "closed" | "holiday";
  timezone: "Asia/Shanghai";
  updatedAt: string;
  items: Record<string, CnMarketItem>;
  errors: Record<string, string>;
};
```

接口要求：

- 最多接收 30 个六位代码，去重并校验输入。
- 单个代码失败不能导致整批失败。
- 优先东方财富，必要时腾讯降级；保留当前超时机制。
- 日内线只为 `instrumentType === "stock"` 且前端请求需要的代码返回，避免所有基金都拉分钟数据。
- 历史日线至少请求 260 个交易日，过滤无效值后取最后 250 个已完成交易日。
- 第三方字段解析集中在 provider adapter 内，不将原始字段暴露给页面。
- 开盘时响应建议短缓存 10–20 秒，收盘后可延长缓存；不得缓存用户持仓内容，只缓存公开行情。

## 5. 前端任务

### Task C：生成请求代码集合

从 `allHoldings` 派生：

```ts
const cnHoldingCodes = allHoldings
  .filter(item => item.market === "A股" || item.market === "基金")
  .map(item => item.symbol);

const dividendStockCodes = allHoldings
  .filter(item => item.market === "A股" && item.category === "红利")
  .map(item => item.symbol);
```

最终是否为股票必须以接口的 `instrumentType` 为准，不能仅凭六位代码判断。

### Task D：开盘刷新策略

新增独立行情状态和请求逻辑，不要混入现有美股行情刷新状态：

- 页面首次打开立即请求。
- A 股交易中每 20 秒更新一次，与现有持仓行情频率一致。
- 11:30–13:00 午休暂停轮询，13:00 立即恢复。
- 15:00 后完成一次收盘刷新并停止高频轮询。
- 页面切到后台时暂停；回到前台立即刷新。
- 使用 `AbortController`，组件卸载或新请求开始时取消旧请求。
- 交易日判断使用 Asia/Shanghai，不使用设备本地时区。
- 周末直接判定休市；法定节假日优先使用行情源返回的市场状态或数据时间判断，不能仅靠星期数声称“交易中”。

### Task E：新增组件

建议在 `app/page.tsx` 中先实现以下组件，避免引入新的大型图表依赖：

```text
CnMarketPanel
CnQuoteList
DividendIntradayCard
IntradayChart
```

复用现有 SVG 走势图思路。日内图注意：

- 上午和下午应分成两段 path，避免把 11:30 和 13:00 画成一段虚假的连续成交。
- Y 轴根据当日真实高低点加少量 padding，不使用固定大范围。
- 无数据、只有一个数据点、部分缺失均不能出现 NaN 或破坏布局。
- 昨收线与走势线要有图例或明确的无障碍标签。

### Task F：样式与手机端

在 `app/globals.css` 中新增独立类名，不覆盖持仓表已有规则：

- 桌面：行情摘要横向网格，红利股走势图两列。
- 手机：全部单列；核心数值字号不低于 14px；操作按钮触控高度不低于 44px。
- 图表宽度使用容器宽度，不产生横向滚动。
- 正涨红、下跌绿；股价黑色；不可用与过期数据使用灰色。
- 与当前 Notion 极简主题一致，不新增高饱和背景和多余装饰。

## 6. 状态与异常处理

必须覆盖：

- 没有 A 股或基金持仓：显示简洁空状态，不隐藏整个布局。
- 有 A 股/基金但没有红利股：显示今日行情摘要，红利股区域显示“当前没有红利类 A 股持仓”。
- 第三方接口部分失败：成功的证券照常显示，失败证券单独标记。
- 休市：显示最后有效数据和时间，不持续刷新。
- 行情陈旧：例如更新时间明显早于当前交易时段，标记“延迟”，不写“实时”。
- MA250 数据不足：显示 `—` 和“历史数据不足”。
- 场外基金净值未更新：显示上一净值日，不把昨日净值写成今日涨跌。

## 7. 测试任务

至少补充以下测试：

1. 沪深代码与证券类型识别。
2. MA250 正数、负数、不足 250 日、无效数据过滤。
3. 09:30、11:30、13:00、15:00 边界和 Asia/Shanghai 时区。
4. 上午、下午日内数据分段。
5. 单个行情失败时批量接口仍返回其他结果。
6. 场外基金不返回伪日内线。
7. 无红利股和完全无中国资产两种空状态。
8. 360px 手机宽度无横向滚动，图表和按钮可操作。
9. TypeScript、构建与现有持仓云同步功能不回归。

## 8. 建议实施顺序

1. 先完成公共 provider 和接口响应类型。
2. 完成证券类型、日内数据和 MA250 计算。
3. 用静态接口响应实现板块基本布局。
4. 接入真实接口与开盘轮询。
5. 完成手机端和异常状态。
6. 补测试、构建验证，再部署 Cloudflare。

## 9. 明确不做

- 不新增自选行情。
- 不展示全市场指数、热力图或新闻。
- 不为场外基金伪造盘中估值线。
- 不把分钟数据写入用户持仓状态或 D1。
- 不更改现有持仓分类、资产配置、收益计算和登录同步逻辑。
- 不引入大型图表库；优先复用现有 SVG 实现。

## 10. 完成标准

- 开盘期间，中国资产行情能按策略自动更新。
- 所有持仓中的红利类 A 股股票均有真实日内线和 MA250 距离。
- 公式、正负号、红绿颜色、时间和基金口径正确。
- 手机和桌面均无横向溢出，页面后台不产生无意义轮询。
- 任一行情源失败不会拖垮整个 Dashboard。
- 云端登录、持仓编辑、自动同步、资产走势和配置饼图保持正常。
